[project]
name = "blastcompare"
version = "0.0.1-pre"
description = "comparing amino acids between pre-aligned seqenes"
authors = [
    {name = "Andrew Robbins", email = "ard114@case.edu"},
]
dependencies = [
    "biopython>=1.75",
    "scikit-bio @ git+https://github.com/theAeon/scikit-bio.git@fb88bd5271bdb74dcec4a88709f50525ad26d59b",
    "parasail>=1.2.4",
]
requires-python = ">=3.8,<3.11"
license = {text = "Apache-2.0"}

[project.urls]
homepage = ""

[project.optional-dependencies]
[tool]
package-dir = "src"
[tool.pdm]
allow_prereleases = true
[tool.pdm.dev-dependencies]
[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"
